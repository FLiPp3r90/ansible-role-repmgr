---
- name: Converge
  hosts: all
  become: true
  gather_facts: true

  roles:
    - role: anxs.postgresql
      vars:
        postgresql_version: 12
        ansible_python_interpreter: "{{ 'python3' if ansible_os_family == 'Debian' else 'python' }}"
    - role: flipp3r90.repmgr
